spring.application.name=TMSBackend

spring.datasource.url=jdbc:mysql://localhost:3306/tms
spring.datasource.username=root
spring.datasource.password=Mypass@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Required - increase these (start with generous values for debugging)
#spring.servlet.multipart.enabled=true
#spring.servlet.multipart.max-file-size=10MB
#spring.servlet.multipart.max-request-size=20MB

# Optional but recommended for form-only (non-file) requests
#spring.servlet.multipart.file-size-threshold=0    
#spring.servlet.multipart.resolve-lazily=true       

# Keep these from before (they help prevent 413 → let Spring handle it)
#server.tomcat.max-swallow-size=50MB
#server.tomcat.max-http-form-post-size=50MB
# or the older name if above doesn't work:
# server.max-http-post-size=50MB
server.tomcat.max-part-count=50
# or higher, e.g. 100 — depending on how many fields you expect max